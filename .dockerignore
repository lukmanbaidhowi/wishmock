# Version control
.git
.gitignore
.github

# Dependencies and build artifacts
node_modules
dist

# Runtime/uploads (mounted at runtime if needed)
uploads

# Tests and coverage (not needed for runtime image)
tests
coverage
.nyc_output
artifacts

# Editor/OS
.vscode
.idea
.DS_Store

# AI agent tooling (local state/logs)
.codex
.cursor
.aider*
codex*.log

# Compose and local files
docker-compose.yml
tmp
.cache
*.swp
*.swo

# Environment files (avoid baking secrets)
.env
.env.*

# Dev-only scripts (only specific scripts are copied)
scripts/*.mjs
scripts/*.sh

!scripts/generate-descriptors.mjs

# Dev-only docs (not needed at runtime)
AGENTS.md
README.md

# Generated descriptor sets (built inside container)
bin/.descriptors.bin
