---
# Rule for BufMessageOneofCheck - oneof validation test
# This method tests oneof required constraints

match:
  request: {}
responses:
  # Response when field 'd' exists
  - when:
      request.d: { exists: true }
    body:
      message: "Oneof validation passed"
    priority: 2
  # Response when field 'e' exists
  - when:
      request.e: { exists: true }
    body:
      message: "Oneof validation passed"
    priority: 2
  # Fallback
  - body:
      message: "Oneof validation passed"
    priority: 1

